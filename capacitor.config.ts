import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.moritzj.musciapps.simplemetronome.app",
  appName: "Simple Metronome",
  webDir: "dist",
  bundledWebRuntime: false
};

export default config;
