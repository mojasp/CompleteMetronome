import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.moritzj.musciapps.completemetronome.app",
  appName: "Complete Metronome",
  webDir: "dist",
  bundledWebRuntime: false
};

export default config;
