{
  "name": "simple-metronome",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/app.ts --outdir=dist --target=browser --format=esm --minify",
    "test": "bun test --dom",
    "typecheck": "bun x tsc --noEmit",
    "watch": "bun build src/app.ts --outdir=dist --target=browser --format=esm --watch"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
